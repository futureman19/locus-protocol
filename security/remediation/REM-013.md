# REM-013: Fix Token Redemption Race Condition

**Fixes:** FINDING-013
**Priority:** LOW
**Effort:** Small (< 1 hour)

## Current Code

```elixir
def redeem_tokens(%City{} = city, token_amount, _redeemer_pubkey) do
  rate = redemption_rate(city.treasury_bsv, city.token_supply)
  bsv_amount = rate * token_amount
  # Check and update on passed-in struct
end
```

## Recommended Fix

Route all redemptions through the GenServer to ensure serialization:

```elixir
def redeem_tokens(city_id, token_amount, redeemer_pubkey) do
  GenServer.call(__MODULE__, {:redeem_tokens, city_id, token_amount, redeemer_pubkey})
end

@impl true
def handle_call({:redeem_tokens, city_id, token_amount, redeemer_pubkey}, _from, state) do
  # Look up the current city state from the GenServer's authoritative copy
  city = Map.get(state.cities, city_id)
  # Perform redemption on the authoritative state
  # Update state atomically within the GenServer
end
```

The GenServer's mailbox naturally serializes all calls, preventing concurrent access to the city state.
